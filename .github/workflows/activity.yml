- name: Test Discord Webhook
  env:
    WEBHOOK: ${{ secrets.DISCORD_ACTIVITY_WEBHOOK }}
  run: |
    echo "Webhook length: ${#WEBHOOK}"

    curl \
      -H "Content-Type: application/json" \
      --data '{"content":"✅ GitHub Actions test"}' \
      "$WEBHOOK"
